{
  version = 1
  sql_table_name = risk_area
  name = risk_area
  columns {
    country {
      label = Country
      description = Country
      type = string
      sql = "{{table}}.country"
    }
    risk_level {
      label = Risk Level
      description = Risk Level
      type = string
      sql = "{{table}}.risk_level"
    }
  }
  aggregates {
    deaths {
      label = Total Death
      description = Total Death
      type = sum
      sql = "{{table}}.deaths"
    }
  }

}